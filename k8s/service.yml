apiVersion: v1
kind: Service
metadata:
  name: p-spring-create-service
  namespace: p-spring-create
spec:
  selector:
    app: p-spring-create
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
